---
title: The factory's empty seams are solved scheduling problems
date: 2026-07-10
updated: 2026-07-11
description: "Applying operations research to Gas City, gascity-packs, and the AOA toolkit: the seams the factory left empty (dispatch, capacity, tier routing, acceptance gates, architecture management) map one for one onto machinery that observatory scheduling and the OR literature already field-tested."
tags: [agents, orchestration, scheduling, operations-research, gas-city]
draft: false
---

*Technical report, 2026-07-10 (implementation status updated 2026-07-11). Companion essay: [The software factory as an observatory](/writing/the-software-factory-as-an-observatory/). The evidence base is a six-agent survey of the SciX/ADS corpus (LP/MIP, network optimization, stochastic and dynamic programming, NLP/duality, modeling languages, metaheuristics), bibcode-anchored; all bibcodes link to ADS. Codebase references are to Gas City (Go core), gascity-packs, and the AOA toolkit as of 2026-07-10.*
---

Gas City's entire pool-dispatch policy is one shell line: `bd ready --metadata-field gc.routed_to=<pool> --unassigned --limit=1` (`config.go:2009-2100`, tier 3 of `EffectiveWorkQuery`). First come, first served, one bead per claim, ordering delegated to whatever `bd` returns. The autoscaler is one line too: desired sessions = ready-queue length, clamped to `[min, max]` (`pool.go:120`, `config.go:2160`). Model selection is a static string in `agent.toml`. There is no lookahead, no load balancing across pool instances (`engdocs/architecture/dispatch.md:318`), no cost model, and no deadline field anywhere in the bead struct (`internal/beads/beads.go:20-36`).

Meanwhile the Zwicky Transient Facility has been running its nightly schedule through an integer linear program since 2018 ([`2019PASP..131f8003B`](https://ui.adsabs.harvard.edu/abs/2019PASP..131f8003B/abstract), 340 citations, still in production), on a problem with the same shape: hundreds of tasks with priorities, time windows, precedence, sequence-dependent switching costs, shared serial resources, fairness constraints across competing programs, and an environment that invalidates the plan several times a night. Las Cumbres shipped the first production ILP telescope-network scheduler in 2014 ([`2015arXiv150307170L`](https://ui.adsabs.harvard.edu/abs/2015arXiv150307170L/abstract)); ALMA re-solves 5,000–10,000 scheduling blocks to optimality with dynamic conditions ([`2016A&C....15...90S`](https://ui.adsabs.harvard.edu/abs/2016A%26C....15...90S/abstract)); the Deep Space Network schedules antenna time with setup/teardown brackets and cross-mission fairness terms ([`2021arXiv211111628C`](https://ui.adsabs.harvard.edu/abs/2021arXiv211111628C/abstract)). Thirty-five years of observatory operations, from SPIKE's constraint satisfaction for Hubble ([`1990aisi.conf....5J`](https://ui.adsabs.harvard.edu/abs/1990aisi.conf....5J/abstract)) onward, have field-tested nearly every mechanism a software factory's scheduler needs.

The claim of this paper: the seams Gas City deliberately left empty, the constants gascity-packs currently fills with hand-tuned values, and the threshold predicates AOA uses as gates are, one for one, the surfaces that operations research solved in those production systems. The corpus survey found zero papers applying any of it to CI systems, merge queues, or agent fleets. The transfer is untraveled, the machinery is proven, and the insertion points already exist as config seams that require no Go edits until late in the adoption path.

## Three systems externalize policy; none optimizes it

Gas City's design invariant is Zero Framework Cognition: "Go handles transport, not reasoning; if a line of Go contains a judgment call, it's a violation" (`engdocs/architecture/glossary.md:166`). Scheduling policy is therefore pushed out through shell seams (`work_query`, `sling_query`, `scale_check`, order `check`, gate `condition`) and prompt text, where it lands as either transport-grade FCFS or model cognition. The packs repo enforces the same split at the policy layer: pack scripts are plumbing, semantic decisions belong in formulas and prompts (`gascity-packs/AGENTS.md:15-24`). What actually fills the seams today is a set of constants and switches: `max_active_sessions = 5` for polecats, `max_active = 1` for the refinery, patrol backoff capped at 300 s, a two-way metadata switch routing `criteria-only` work to Opus and `prescriptive` work to Codex (`mol-dispatch.formula.toml:50-72`), lexicographic sort keys in PR triage ("priority label > recency"), and a wake budget of five sessions per tick (`config.go:1571`).

AOA is the same story at the measurement layer. The toolkit computes four real metrics from agent traces (retrieval locality, edit locality, invariant discoverability, mutation surface; `crates/aoa-metrics/src/lib.rs:23-42`), then makes every decision with a threshold predicate or a lexicographic sort: audit findings rank by `(Tier, descending measured cost, title)` (`crates/aoa-audit/src/punch.rs:122-129`), migrations are admitted one at a time when `held_out_delta > 0 && gap_delta <= 0` (`crates/aoa-gap/src/compare.rs:6-16`), and the R0 gate is a majority vote over at least five repos (`crates/aoa-falsify/src/verdict.rs:42-79`). A targeted grep across the workspace finds no scheduling, routing, bandit, or multi-objective machinery at all.

None of this is an accident, and the fix is not to violate ZFC. ZFC's own allowed list includes deterministic math, policy enforcement, and mechanical transforms. A priority index computed from declared features, a MILP solved over declared constraints, a bandit updating declared posteriors: these are deterministic mechanisms over versioned data, closer in spirit to `clamp(desired, min, max)` than to a hidden heuristic. The judgment stays where ZFC wants it, in what the model writes into the model; the solving becomes transport. The Bitter Lesson test passes for the same reason: solvers (HiGHS, SCIP, CP-SAT) improve on their own compute curve independent of the LLM, and the LLM's role in the loop (authoring and revising the declarative model) gets better as models improve. There is already a published existence proof of exactly this division of labor: MCP-Solver ([`2025arXiv250100539S`](https://ui.adsabs.harvard.edu/abs/2025arXiv250100539S/abstract)) has an LLM edit a MiniZinc model item by item, every edit validated before acceptance, solve strictly separated from edit, model state held server-side. That is the ZFC write path, built by someone else, fourteen months ago.

## Observatories already ran the experiment the factory is about to run

The survey's most transferable results are negative and comparative, the kind that save years. The field split into two camps on the same design tension Gas City faces: ZTF re-solves a deterministic ILP as conditions change, while Rubin/LSST rejected explicit planning entirely in favor of a memoryless feature-based policy ([`2019AJ....157..151N`](https://ui.adsabs.harvard.edu/abs/2019AJ....157..151N/abstract)) that scores every candidate as a weighted sum of handcrafted features and re-decides at every step, absorbing weather loss for free because any state is a valid restart point. A decade of operations vindicated both over scenario-tree stochastic planning, which appears in this literature only as a 2025 preprint ([`2025arXiv250403666R`](https://ui.adsabs.harvard.edu/abs/2025arXiv250403666R/abstract)). The lesson reads directly onto Gas City's patrol loops: cheap re-decision on current state, which the wisp architecture already does, beat explicit uncertainty modeling in every deployed system. The OR upgrade is not "add a stochastic planner"; it is "make the re-decision informed."

Two more results constrain the build. Bold and Goerigk's compact robust-RCPSP formulation solves 93.1% of benchmark instances to proven optimality against 65.0% for an improved Benders decomposition, at 100–200× the speed ([`2022arXiv220306983B`](https://ui.adsabs.harvard.edu/abs/2022arXiv220306983B/abstract)); price-based decomposition ([`2022NatSR..1222417B`](https://ui.adsabs.harvard.edu/abs/2022NatSR..1222417B/abstract)) only pays off at genuinely large separable scale. A factory queue of 10²–10⁴ beads fits in one open-source solver process. And every production astronomy MILP runs anytime: hard time limit, ship the incumbent, treat the optimality proof as a luxury (MUSHROOMS caps at 500 s and accepts 2–11% gaps where proofs don't close, [`2022ApJ...935...87P`](https://ui.adsabs.harvard.edu/abs/2022ApJ...935...87P/abstract)). The plan will be stale before the proof lands; the observatories stopped paying for proofs years ago.

## Dispatch: a priority index in the work_query seam

The lowest-friction insertion in the entire stack is replacing tier 3 of `EffectiveWorkQuery` with a ranked claim. This is a pure config change per Gas City's own design (`config.go:2009` is fully overridable per agent), and the replacement policy is Naghib's LSST scheduler transplanted: score each ready bead as a weighted sum of features (age, priority, dependency criticality measured as downstream unblocked work, expected cost for this task class, retry count, historical success rate for task-type × model tier), claim the argmax, tune the weights offline by black-box optimization against replayed queue traces. Evaluation costs microseconds, no transition model is needed, and the policy inherits the crash-robustness property that made it survive weather: a polecat that dies mid-claim leaves a state from which the next claim is just as valid.

Deadline-shaped work wants one refinement. Yu, Xu, and Tong showed that scheduling jobs with deadlines and stochastic service times decomposes into a per-job Whittle index, a single priority scalar that near-optimally trades urgency against completion probability ([`2016arXiv161000399Y`](https://ui.adsabs.harvard.edu/abs/2016arXiv161000399Y/abstract)). The bead model currently has no deadline field (`beads.go:20-36` carries `Priority *int` and nothing temporal), which is the one schema gap worth closing early: a `gc.due` metadata key, or a first-class field, is the prerequisite for any urgency-aware index and costs nothing to add now.

## Epoch planning: the bead DAG is a resource-constrained project schedule

Between claims, the factory periodically wants a plan, and the bead graph is literally an RCPSP instance: activities with finish-to-start precedence (`Dependencies []Dep`), renewable resources (agent seats per pool, refinery slots, CI runners), and non-renewable budgets (daily tokens). The multi-mode variant maps model-tier routing into the same solve: each bead offers modes (Opus, Sonnet, Haiku, Codex) with different duration and cost, and the solver picks mode and schedule jointly, which turns `mol-dispatch`'s two-way routing switch into data. Duration uncertainty gets the Bertsimas-Sim budgeted treatment, guarding the makespan against "at most Γ beads blow up simultaneously" instead of the useless all-worst-case ([`2022arXiv220306983B`](https://ui.adsabs.harvard.edu/abs/2022arXiv220306983B/abstract)). The natural host is an `internal/orders` exec order on a cooldown trigger, recomputing assignments each epoch and re-slinging; the order machinery exists (`internal/orders/triggers.go:46`) and runs agentless on the controller tick.

Three formulation pieces from the observatory papers drop in with almost no translation. Lampoudi's reservation-start binaries, where Y_ik = 1 means reservation i starts at slot k and an occupancy map enforces one reservation per resource-slot, model the merge queue and CI-slot booking in |I|+|T| constraint rows ([`2015arXiv150307170L`](https://ui.adsabs.harvard.edu/abs/2015arXiv150307170L/abstract)); the refinery's `max_active = 1` becomes a capacity row instead of a lock. Singer's disjunctive interval constraints, |t_a − t_b| ≥ δ with an order binary ([`2025PASP..137g4501S`](https://ui.adsabs.harvard.edu/abs/2025PASP..137g4501S/abstract)), turn worktree conflict avoidance from today's pessimistic serialization (per-bead worktree, then the refinery serializes every merge) into a constraint the solver plans around: two beads whose file sets overlap simply must not overlap in time, and the solver decides who goes first by what the objective prefers. And ZTF's two-level decomposition, an assignment ILP over 30-minute blocks followed by an exact tour within each block, is the template for session batching: assign beads to agent-session blocks, then order work inside the session to minimize context-switch cost, because a warm worktree, a loaded repo map, and cached build state are slew time under another name.

The operational discipline comes from ALMA and the MIPcc23 reoptimization competition. ALMA re-solves on every condition change but treats an in-flight scheduling block as atomic; the factory equivalent freezes running sessions as fixed constraints and never preempts them. MIPcc23 ([`2023arXiv231114834B`](https://ui.adsabs.harvard.edu/abs/2023arXiv231114834B/abstract)) defines the warm-start regime the factory lives in, one formulation with perturbed data every tick, and its scoring makes schedule stability a measured quantity rather than a hope, which matters because a planner that reshuffles everyone's assignments every epoch is worse than FCFS in practice even when its makespan is better on paper.

## Capacity is a queueing problem; tier routing is a budgeted bandit

The `scale_check` seam currently implements a proportional controller with no plant model: desired = queue length. Merge and review pipelines run intentionally hot, and the regime for sizing intentionally-hot service systems is heavy-traffic queueing, including the recent variant where service-time distributions themselves are unstable ([`2022arXiv220405733A`](https://ui.adsabs.harvard.edu/abs/2022arXiv220405733A/abstract)), which is the honest description of agent runtimes across model versions. The concrete near-term wins are cheaper than that machinery suggests: hysteresis and dwell times for the unimplemented anti-flapping cooldowns (`engdocs/design/agent-pools.md`, Phase 4), and a two-settlement structure for capacity, a committed baseline plus burst recourse, which is structurally the demand-response aggregator MDP ([`2016ecc..conf..204R`](https://ui.adsabs.harvard.edu/abs/2016ecc..conf..204R/abstract)) and, in its simplest form, an adaptive two-stage stochastic program whose bounds say one revision point captures most of the value of full multistage planning when demand drifts slowly ([`2019arXiv190603513B`](https://ui.adsabs.harvard.edu/abs/2019arXiv190603513B/abstract)). The static constants worth promoting to adaptive control are already enumerable: `DefaultMaxWakesPerTick=5` (`config.go:1571`), three stops and sixteen interrupts per lifecycle wave (`session_lifecycle_parallel.go:34-35`).

Tier routing has a stronger claim waiting. `internal/pricing` already produces per-invocation cost estimates and is explicitly scoped as decision support (`pricing.go:1-16`); nothing consumes it for decisions. Each (task-class, tier) pair is an arm with random cost in tokens and wall-time and random reward in merged outcomes, and maximizing merges before the budget dies is the budget-constrained bandit objective exactly ([`2020arXiv200300365C`](https://ui.adsabs.harvard.edu/abs/2020arXiv200300365C/abstract); knapsack-bandit chapter of [`2019arXiv190407272S`](https://ui.adsabs.harvard.edu/abs/2019arXiv190407272S/abstract)). Thompson sampling on reward-per-cost replaces the static routing table, degrades gracefully to it under thin data, and inherits AOA's construct-validity discipline for free: the reward definition ("merged and not reverted within N days") is a declared, versioned choice, advisory until its correlation with an external outcome is confirmed, exactly as `crates/aoa-gap/src/construct.rs` already does for metrics.

## Acceptance gates: noisy constraints, documented Goodhart, unwired finalizer

Gas City has a sanctioned seam waiting for a deterministic acceptance rule: the review-quorum finalizer defines the durable verdict contract but "is not invoked by formula synthesis yet" (`internal/reviewquorum/finalize.go`; `dispatch.md:266`). Two survey results say what to put there. First, gate signals are noisy, because flaky tests, nondeterministic benchmarks, and LLM-judge scores are noisy evaluations of the underlying constraint, and the noisy-SQP literature ([`2021arXiv211004355O`](https://ui.adsabs.harvard.edu/abs/2021arXiv211004355O/abstract)) supplies acceptance criteria that don't thrash on measurement noise; a verdict rule that requires signal exceeding a noise-calibrated margin, rather than a single reading, is the direct transfer. Second, the acceptance predicate should be dominance, not a scalar. Chen, Li, and Yao showed weighted-sum fitness converges to points that Pareto search dominates ([`2020arXiv200108236C`](https://ui.adsabs.harvard.edu/abs/2020arXiv200108236C/abstract)), and AOA's own `Label::Good` (improves held-out pass AND holds-or-shrinks the reward-hacking gap, `compare.rs:6-16`) is already a two-objective dominance check; the factory-wide version keeps {tests, diff size, coverage delta, review findings} as a vector and accepts on dominance within tolerance.

The Goodhart evidence deserves standing citation because it is the strongest empirical support for a design decision all three systems already made. At Adyen, 5.5M LOC, search operators learned to game the modularity metric monotonically by moving single classes into fresh modules, metric improvement did not predict developer acceptance, and developer review had to be the real gate ([`2021arXiv210200701S`](https://ui.adsabs.harvard.edu/abs/2021arXiv210200701S/abstract)). Metric gates select candidates; an independent judge that was not the optimization target accepts them. Gas City's refinery-rejects-to-pool loop, the pr-ship formula's three-iteration convergence cap (`mol-pr-ship.formula.toml:156-247`), and AOA's held-out conditioning are all instances of the same defense, now with a published industrial failure mode behind it.

Gas City's NDI stance, redundant independent attempts as the reliability mechanism with premature convergence "intentionally tolerated," also picks up quantitative footing here. Gravitational-wave searches run multiple independent particle swarms specifically to bound the probability of missing the global peak ([`2010PhRvD..81f3002W`](https://ui.adsabs.harvard.edu/abs/2010PhRvD..81f3002W/abstract)), which is the argument for N no-crosstalk agent attempts merged only at selection, stated with a number attached; and PIKAIA's convergence detector, population fitness contrast driving the mutation rate ([`1995ApJS..101..309C`](https://ui.adsabs.harvard.edu/abs/1995ApJS..101..309C/abstract)), is a ready-made alarm for the failure mode where parallel attempts have all collapsed onto the same local fix and the fleet should raise its temperature through prompt variance, model mix, or fresh context seeds.

## AOA supplies the objective vector; OR hardens its gates

The relationship between AOA and the optimization layer runs both directions. Forward: AOA's four metrics are the best available candidates for what the factory's architecture-management objective vector should contain, because they are trace-grounded measurements of agent-legibility rather than proxies for human taste, and they come with declared parameters (Recall@k's k, mutation depth, floor/ceiling) already emitted as data. A boundary-placement optimizer needs edge weights; retrieval and edit locality, computed per module from real traces, are principled coupling weights for the graph work in the next section, which is a materially better story than co-change counts alone.

Backward: the premortem's central finding, six independent failure lenses all breaking R0, the single blocking gate, with the shared theme that "declaring the over-approximation as data makes ambiguity visible without making verdicts robust" (`premortem_aoa_toolkit.md:16-26`), is a problem with known OR shapes. A majority vote over five repos is a point verdict; the alternatives are interval verdicts with certificates. Duality gives bounds (this migration's held-out improvement is at least X under the declared weights, at most Y under the adversarial ones); flat-truncation-style certificates ([`2011arXiv1106.2384N`](https://ui.adsabs.harvard.edu/abs/2011arXiv1106.2384N/abstract)) model the pattern of proving a verdict insensitive to the contested parameter choices rather than asserting it; and the rankings-flip-under-floor-vs-ceiling failure in Theme B is precisely what reporting a dominance region instead of a scalar rank prevents. The migrate roadmap's one-at-a-time admission policy ("never batch," each candidate admitted only on `Label::Good`) is a sequential decision process currently run as a fixed ordering; with reward = the Good label and cost = evaluation spend, it is a budgeted bandit over the CodeFix candidate set, and the exploration it adds is exactly what a fixed ordering forecloses.

One correspondence is close enough to be checked rather than argued. `aoa-budget` enforces a token ceiling over the transitive closure of context files; the SKA's DALiuGE scheduler ([`2018arXiv180507568W`](https://ui.adsabs.harvard.edu/abs/2018arXiv180507568W/abstract)) partitions a dataflow DAG under the constraint that concurrently-active resource demand per partition stays under node capacity, and its published negative result is that partitioning the whole static graph is both wasteful and ill-posed because only the temporal working set matters. Same constraint, same warning: the unit of architecture optimization is the active frontier of the repo under an agent's context capacity, not the global dependency graph.

## Architecture management: cluster with Leiden, place boundaries with min-cut, restructure like basin-hopping

For the codebase itself, the survey's graph results give the deterministic half of an architecture loop. Community detection over the symbol/file dependency graph, weighted by AOA's locality metrics, proposes module and ownership boundaries; the algorithm choice matters because Louvain can emit disconnected communities, which as package boundaries are nonsense, while Leiden guarantees connectivity ([`2019NatSR...9.5233T`](https://ui.adsabs.harvard.edu/abs/2019NatSR...9.5233T/abstract)), and refactoring-via-community-detection is published practice ([`2018arXiv181110171R`](https://ui.adsabs.harvard.edu/abs/2018arXiv181110171R/abstract)). Boundary placement between two specific subsystems is a min-cut query, and the almost-linear max-flow result ([`2022arXiv220300671C`](https://ui.adsabs.harvard.edu/abs/2022arXiv220300671C/abstract)) makes per-commit min-cut affordable at monorepo scale; the survey found no paper applying flow-based boundaries to software, so this transfer is claimable. Edit scripts, not target states, are the representation for the resulting refactoring plans, a choice three literatures converged on independently (remodularization, Adyen, program repair [`2017arXiv171207804Y`](https://ui.adsabs.harvard.edu/abs/2017arXiv171207804Y/abstract)) because incremental fitness on diffs is cheap and plans map one-to-one onto reviewable PR-sized changes, which is also the representation `aoa-migrate`'s CodeFix plans already use.

For restructuring that greedy agents can't reach, the chemical-physics energy-landscape corpus is the underexploited source. Funnel topography predicts difficulty ([`2000cond.mat..7338D`](https://ui.adsabs.harvard.edu/abs/2000cond.mat..7338D/abstract)): a single-funnel codebase improves safely under greedy local cleanup, while a multi-funnel one, several plausible decompositions separated by large-diff barriers, traps greedy agents in locally-clean-but-globally-wrong states. Basin-hopping ([`1998cond.mat..3344W`](https://ui.adsabs.harvard.edu/abs/1998cond.mat..3344W/abstract)) is the protocol for crossing: take a deliberately disruptive perturbation, immediately run local cleanup, and accept or reject on the post-cleanup score, never the raw perturbed one. That acceptance rule is the principled version of "let an agent attempt the big move, then judge the cleaned-up result," and it is what justifies temporarily holding a worse intermediate state inside a molecule whose gate only fires after the cleanup step.

## Packs already separate model from data; add the solver and the certificates

The packs repo has, without naming it, rebuilt the algebraic-modeling-language pattern that Pyomo and JuMP exist to provide ([`2015arXiv150801982D`](https://ui.adsabs.harvard.edu/abs/2015arXiv150801982D/abstract)): declarative manifests and formula variables hold the model, city state holds the data, executable code is forbidden from holding policy, and composition is by import. What is missing is the two things an AML buys beyond structure. The first is a solve step: the constants in `agent.toml` and the routing predicates in formulas become decision variables and constraints the epoch solver reads, so a cap like `max_active_sessions = 5` stops being a hand-tuned guess and becomes either a solver output or a declared hard constraint with a known shadow price. The second is infeasibility certificates. When the declared rules admit no schedule, an exact solver emits an irreducible infeasible subsystem, the minimal conflicting constraint set, turning "the town is stuck" into "these three rules cannot hold simultaneously; relax one." Pack name collisions already fail with a clear diagnosis ("a collision, not a composition," `docs/design/slack-pack-tiering.md:158-189`); IIS extends that courtesy to every policy conflict, and the survey found IIS underused even among OR practitioners, so a factory that surfaces conflict certificates routinely is ahead of the field that invented them. Solver logs complement the event bus the same way: bounds, gaps, and timing per decision, replayable and appealable, where model judgment offers neither.

Shadow prices close the economics loop, with the documented caution attached. At an epoch optimum, the dual on each binding constraint prices what relaxing it buys: another refinery slot, another CI runner, another point of token budget. Khabarov showed extrapolating those multipliers over finite changes produces "an arbitrary number" ([`2022arXiv221103591K`](https://ui.adsabs.harvard.edu/abs/2022arXiv221103591K/abstract)), so the discipline is relax one constraint, re-solve, read the new prices; the factory gets a principled answer to "what should we buy next" without ever trusting a multiplier beyond the margin.

## Evaluation: replay fixed traces and beat the dumb baseline first

Nothing above should ship on argument alone, and the survey's most portable finding is the evaluation discipline, not any formulation. Decima's variance-reduction trick ([`2018arXiv181001963M`](https://ui.adsabs.harvard.edu/abs/2018arXiv181001963M/abstract)) generalizes past RL entirely: when comparing two dispatch policies on an input-driven system, replay the identical recorded arrival trace under both, because otherwise queue-arrival noise swamps the policy signal. Gas City records everything needed for this already; beads and the event bus are the trace. The second gate is SWAY ([`2016arXiv160807617C`](https://ui.adsabs.harvard.edu/abs/2016arXiv160807617C/abstract)): random oversampling plus recursive halving matches evolutionary search at orders of magnitude fewer evaluations across SBSE benchmarks, and its authors proposed it as the mandatory baseline precisely because clever optimizers embarrassingly often tie it. Every claim that the MILP or the bandit beat FCFS must also report the margin over a trivially-tuned priority queue on the same replayed traces. Third, LCO validated its scheduling kernel on constructed instances with known optima, oversubscribed and undersubscribed regimes both, before trusting it; the factory's scheduler deserves the same fixture set. And exact methods earn their keep on negatives: in Handley's runs the heuristic failed to schedule 7 of 360 instances; six were genuinely infeasible, and on the seventh the MILP proved a feasible schedule the heuristic had silently missed ([`2024AJ....167...33H`](https://ui.adsabs.harvard.edu/abs/2024AJ....167...33H/abstract)), which is the same silent-loss class as the dispatch-drop risk the packs architecture already flags (`gascity-packs/architecture/README.md:79-83`). Solvers certify; heuristics shrug.

## What not to build

The negative space is as load-bearing as the proposals. No scenario-tree stochastic planner: the deployed systems that faced the most quantifiable uncertainty in this literature chose deterministic re-solve or memoryless policies, and the factory should exhaust those before touching recourse models. No Benders or price decomposition before a compact model has actually hit a wall; at bead-queue scale the monolith wins by two orders of magnitude. No solver in the hot loop: the index dispatches in microseconds, the MILP runs on epochs with a hard time limit and ships incumbents. No scalar architecture-health score, ever; the weighted sum has a published failure mode and AOA's dominance-shaped `Label::Good` is already the right pattern. No shadow-price arithmetic beyond the margin. No commercial-solver dependency: the astronomy literature runs almost entirely on Gurobi academic licenses and never benchmarks alternatives, a reproducibility monoculture worth not importing when HiGHS, SCIP, and CP-SAT are open, strong, and consistent with this stack's no-paid-API posture. And no belief in any of it without the replay harness; the evaluation section is a prerequisite, not an appendix.

## Adoption in four phases, config-first

| Phase | Change | Seam | Code touched |
|---|---|---|---|
| 0 | Trace replay harness; deadline metadata (`gc.due`); SWAY/FCFS baselines; known-optimum fixture instances | beads + event bus (read-only) | none (tooling only) |
| 1 | Feature-based priority index replacing FCFS claim; weights tuned on replayed traces | `work_query` / `sling_query` (`config.go:2009`) | none (config + one scoring script) |
| 2 | Epoch MILP (RCPSP + reservation binaries + disjunctive worktree windows + tier modes), anytime, warm-started; stability scored | `internal/orders` exec order; refinery capacity as constraint rows | new order + solver sidecar (HiGHS/CP-SAT) |
| 3 | Budgeted-bandit tier router fed by `internal/pricing`; deterministic quorum finalizer with noise-margin verdicts | `MolCook` formula vars or session-provider build; `reviewquorum/finalize.go` | Go: wire finalizer, add routing hook |
| 4 | Architecture loop: Leiden/min-cut boundaries weighted by AOA locality metrics; basin-hopping restructure protocol; IIS surfacing for policy conflicts | AOA metrics pipeline + migrate CodeFix plans | new analysis tooling; AOA stays measurement-side |

Phases 0 and 1 are deliberately boring: they change no Go, they produce the measurement substrate everything later is judged against, and phase 1 alone, on the observatory evidence, is where most of the value of "informed re-decision over FCFS" lives. Each subsequent phase is admitted the way AOA admits migrations, one at a time, on replayed-trace evidence against the phase-0 baselines, never batched.

The open experiment is the one AOA already knows how to frame. R0 asks whether repo structure moves agent outcomes more than harness choice does; the scheduling analog asks whether solver-informed dispatch moves factory throughput and latency more than the dumb baseline does, on identical replayed traces, across enough recorded weeks to power the comparison. The corpus says nobody has published that experiment for software factories. The instruments to run it are three config files and a replay script away.

## Where the Gas City implementation stands (2026-07-11)

Phase 1 has a complete, ready-to-implement design spec as of July 11: a priority-index dispatch policy in the `work_query` seam. The spec re-verified this paper's premises against the current tree and sharpened two of them. The seam has moved (`config.go:2009` is now `internal/config/workquery.go:294`), and a custom `work_query` replaces the *full three-tier discovery contract*, not just tier 3 — so the scoring script reproduces the crash-recovery and pre-assigned tiers verbatim and changes claim order only. The claim contract turns out to be exactly "first eligible element of the JSON array the script prints," which means the entire dispatch policy is the sort order of that array.

The score is Rubin's shape transplanted without modification: `S(b) = 1.00·f_prio + 0.10·f_due + 0.08·f_unblock + 0.06·f_age`, four features normalized to [0, 1] from fields `bd ready --json` already returns. The initial weights encode a band-dominance invariant: adjacent priority bands sit 0.25 apart and the secondary weights sum to 0.24, so declared priority strictly dominates and the index only reorders *within* a band — which is where FCFS was actually leaving value (an aged, heavily-blocking, due-tomorrow P2 sitting behind a fresh isolated one). The missing deadline field lands as a `gc.due` metadata convention read only by the scoring script, never enforced. Rollout is three gated stages: shadow mode (log what would have been claimed, change nothing), a one-rig canary, then fleet, each admitted on replayed-trace evidence, and FCFS is a point in the weight space, so the tuned index can only lose to it by overfitting, which the temporal holdout catches.

Phase 0 is the current frontier. The workspace's event bus holds about 7.5 weeks of full bead snapshots (2026-05-19 onward), enough to replay recorded arrivals under FCFS, a trivially-tuned priority sort, and the index on identical traces, reporting priority-weighted flow time, unblocking-value throughput, and per-class p95 tails. That replay harness (`bin/or-replay`) is the first artifact to build: it gates everything else, produces the baseline numbers this factory has never measured, and needs no approvals to run read-only. Nothing is in production yet — dispatch fleet-wide today is still the one-line FCFS this paper opened with.

---

## References

**Corpus survey (full detail, all sections):** `scix_experiments/docs/or_optimization_survey_2026-07.md`

Key bibcodes cited above, resolvable at ADS: [`1990aisi.conf....5J`](https://ui.adsabs.harvard.edu/abs/1990aisi.conf....5J/abstract) (SPIKE/HST), [`1995ApJS..101..309C`](https://ui.adsabs.harvard.edu/abs/1995ApJS..101..309C/abstract) (PIKAIA GA), [`1998cond.mat..3344W`](https://ui.adsabs.harvard.edu/abs/1998cond.mat..3344W/abstract) (basin-hopping), [`2000cond.mat..7338D`](https://ui.adsabs.harvard.edu/abs/2000cond.mat..7338D/abstract) (funnel topography), [`2011arXiv1106.2384N`](https://ui.adsabs.harvard.edu/abs/2011arXiv1106.2384N/abstract) (flat-truncation certificates), [`2015arXiv150307170L`](https://ui.adsabs.harvard.edu/abs/2015arXiv150307170L/abstract) (Las Cumbres ILP), [`2015arXiv150801982D`](https://ui.adsabs.harvard.edu/abs/2015arXiv150801982D/abstract) (JuMP), [`2016A&C....15...90S`](https://ui.adsabs.harvard.edu/abs/2016A%26C....15...90S/abstract) (ALMA), [`2016arXiv160807617C`](https://ui.adsabs.harvard.edu/abs/2016arXiv160807617C/abstract) (SWAY), [`2016arXiv161000399Y`](https://ui.adsabs.harvard.edu/abs/2016arXiv161000399Y/abstract) (deadline restless bandits), [`2016ecc..conf..204R`](https://ui.adsabs.harvard.edu/abs/2016ecc..conf..204R/abstract) (two-settlement MDP), [`2017arXiv171207804Y`](https://ui.adsabs.harvard.edu/abs/2017arXiv171207804Y/abstract) (ARJA), [`2010PhRvD..81f3002W`](https://ui.adsabs.harvard.edu/abs/2010PhRvD..81f3002W/abstract) (independent-swarm PSO), [`2018arXiv180507568W`](https://ui.adsabs.harvard.edu/abs/2018arXiv180507568W/abstract) (DALiuGE), [`2018arXiv181001963M`](https://ui.adsabs.harvard.edu/abs/2018arXiv181001963M/abstract) (Decima), [`2018arXiv181110171R`](https://ui.adsabs.harvard.edu/abs/2018arXiv181110171R/abstract) (remodularization via community detection), [`2019AJ....157..151N`](https://ui.adsabs.harvard.edu/abs/2019AJ....157..151N/abstract) (LSST feature scheduler), [`2019PASP..131f8003B`](https://ui.adsabs.harvard.edu/abs/2019PASP..131f8003B/abstract) (ZTF ILP), [`2019arXiv190407272S`](https://ui.adsabs.harvard.edu/abs/2019arXiv190407272S/abstract) (bandits survey), [`2019arXiv190603513B`](https://ui.adsabs.harvard.edu/abs/2019arXiv190603513B/abstract) (adaptive two-stage SP), [`2019NatSR...9.5233T`](https://ui.adsabs.harvard.edu/abs/2019NatSR...9.5233T/abstract) (Leiden), [`2020arXiv200108236C`](https://ui.adsabs.harvard.edu/abs/2020arXiv200108236C/abstract) (SBSE weighted-sum failure), [`2020arXiv200300365C`](https://ui.adsabs.harvard.edu/abs/2020arXiv200300365C/abstract) (budgeted bandits), [`2021arXiv210200701S`](https://ui.adsabs.harvard.edu/abs/2021arXiv210200701S/abstract) (Adyen remodularization), [`2021arXiv211004355O`](https://ui.adsabs.harvard.edu/abs/2021arXiv211004355O/abstract) (noisy SQP), [`2021arXiv211111628C`](https://ui.adsabs.harvard.edu/abs/2021arXiv211111628C/abstract) (DSN Δ-MILP), [`2022ApJ...935...87P`](https://ui.adsabs.harvard.edu/abs/2022ApJ...935...87P/abstract) (MUSHROOMS), [`2022NatSR..1222417B`](https://ui.adsabs.harvard.edu/abs/2022NatSR..1222417B/abstract) (SLBLR), [`2022arXiv220300671C`](https://ui.adsabs.harvard.edu/abs/2022arXiv220300671C/abstract) (almost-linear max flow), [`2022arXiv220306983B`](https://ui.adsabs.harvard.edu/abs/2022arXiv220306983B/abstract) (compact robust RCPSP), [`2022arXiv220405733A`](https://ui.adsabs.harvard.edu/abs/2022arXiv220405733A/abstract) (high-uncertainty heavy traffic), [`2022arXiv221103591K`](https://ui.adsabs.harvard.edu/abs/2022arXiv221103591K/abstract) (shadow-price caution), [`2023arXiv231114834B`](https://ui.adsabs.harvard.edu/abs/2023arXiv231114834B/abstract) (MIPcc23 reoptimization), [`2024AJ....167...33H`](https://ui.adsabs.harvard.edu/abs/2024AJ....167...33H/abstract) (Traveling Telescope Problem), [`2025arXiv250100539S`](https://ui.adsabs.harvard.edu/abs/2025arXiv250100539S/abstract) (MCP-Solver), [`2025PASP..137g4501S`](https://ui.adsabs.harvard.edu/abs/2025PASP..137g4501S/abstract) (UVEX/M4OPT), [`2025arXiv250403666R`](https://ui.adsabs.harvard.edu/abs/2025arXiv250403666R/abstract) (scheduling under night-loss uncertainty).

Codebase references: Gas City core (`cmd/gc/`, `internal/`, `engdocs/`), gascity-packs (`gastown/`, `pr-pipeline/`, `.beads/formulas/`, `docs/design/slack-pack-tiering.md`, `AGENTS.md`), AOA (`crates/aoa-*`, `prd_aoa_toolkit.md`, `premortem_aoa_toolkit.md`, `docs/migrate-roadmap.md`).
